# lint-md-cli-rust

Experimental Rust validation prototype for a native `lint-md` command-line interface.

Implementation work is developed through pull requests so the TypeScript CLI can remain the compatibility reference.
